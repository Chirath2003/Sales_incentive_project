"# Sales_incentive_project" 

## 🐍 Backend Setup

1. Create a virtual environment:
   ```bash
   python -m venv .venv

2. Activate it
    .venv\Scripts\activate

3. install dependencies
    pip install -r requirements.txt
